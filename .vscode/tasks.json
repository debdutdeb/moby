{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build daemon",
            "type": "shell",
            "command": "go build -gcflags=all='-N -l' -o ${workspaceFolder}/dockerd ${workspaceFolder}/cmd/dockerd",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}